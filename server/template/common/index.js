const scriptTags = require('./scripts');
const linkTags = require('./links');
const metaTags = require('./meta');
const initialData = require('./initialData');

module.exports = {
	scriptTags,
	linkTags,
	metaTags,
	initialData,
}